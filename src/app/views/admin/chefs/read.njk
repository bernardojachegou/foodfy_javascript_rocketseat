{% extends "layout.njk" %}

{% block content %}
    <div class="recipe_details">
        <div class="recipe_options">
            <h1>Chef:
                {{chef.name}}</h1>
            <a class="crud_button" href="/admin/chefs/{{chef.id}}/editar">Editar chef</a>
        </div>
        <div class="chef-box-img">
            <span style="background-image: url({{chef.avatar_url}})"></span>
            <h1>{{chef.name}}</h1>
            <p>{{chef.total_recipes}} receitas</p>
        </div>

        {# <section class="admin-cards">
            <h2>Receitas</h2>
            {% for recipe in chef.total_recipes %}
                <div class="admin-card" id="{{recipe.id}}">
                    <div class="admin-card-img">
                        <img src="{{recipe.image}}" alt="{{recipe.title}}">
                    </div>
                    <div class="admin-card-info">
                        <h3>{{recipe.title}}</h3>
                        <p id="author_name">{{chef.name}}</p>
                        <a href="/admin/receitas/{{recipe.id}}">Vizualizar</a>
                    </div>
                </div>
            {% endfor %}
        </section> #}

    </div>
{% endblock %}